/** @type {import('next').NextConfig} */
const nextConfig = {
  /* config options here */
  reactCompiler: true,
   images: {
    domains: ['headless.zbinfo.site'],
  },
};

export default nextConfig;
